<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= render :partial => 'loyal_passport/users/registrations/new', :locals => {:f => f} %>
<% end %>

<%= render "loyal_passport/users/shared/links" %>
